# Reference asset status

The reference crawl recovered both Home diagrams and eight member portraits. The reference does not display a portrait for `sanghyeok lee`, so the clone uses a neutral initials placeholder in the same square ratio.

If an approved portrait becomes available, add it to `public/assets/images/members/`, set the `image` and `imageAlt` fields in `src/data/members.ts`, and rebuild the site.
